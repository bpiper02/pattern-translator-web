# Focused audio pass

This branch now includes a post-merge focused pass that replaces the old drum detector with Essentia SuperFlux onset detection, adds rendered translated preview playback, and prepares the separation service boundary.
